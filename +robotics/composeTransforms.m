function transformOut = composeTransforms(transform1,transform2)
%COMPOSETRANSFORMS Take 2d transforms and combine them into a single
%transform
%   For planar motion, the composition of two transforms is formulaic, but
%   does not have a binary operation equivalent for standard scalar math.

end

